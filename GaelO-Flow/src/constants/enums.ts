export enum ProcessingMask {
  RTSS = 'rtss',
  SEG = 'seg',
  NIFTI = 'nifti',
}

export enum ProcessingJobType {
  TMTV = 'tmtv',
}

export enum TagPolicies {
  KEEP = 'Keep',
  REPLACE = 'Replace',
  REMOVE = 'Remove',
}

export enum QueryAnswerType {
  LEVEL_SERIES = 'series',
  LEVEL_STUDY = 'study',
}
